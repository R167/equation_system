require 'spec_helper'

describe EquationSystem do
  it 'has a version number' do
    expect(EquationSystem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
